# 2013-11-25	

UberLameKit, the following dependecies are required to function:

+ Python 2.6x + bcrypt + crypt + optparse + thread + pexpect +

file > master.py

Currently this program has the ability to: brute force algorithms, brute force tcp services, etc

-E <engine>
	* unhash
	* bruteforce
-D <dictionary>
-T <target file>
-U <target username>
-H <target hostname>
-A <attack type>

IE: To begin cracking encrypted passwords with unhash
./master.py -E unhash -D dictionary.txt -T salt.txt

IE: To begin brute forcing with secure shell accounts
./master.py -E bruteforce -A ssh -U root -D dictionary.txt
